RewriteEngine On

# Don't rewrite existing files or folders
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Pretty URLs
RewriteRule ^([a-zA-Z0-9-]+)/?$ hotel.html?hotel=$1 [L,QSA]